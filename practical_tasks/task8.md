# Основные правила и формулы вычислений

---

## 1-5 Вычисление длины вектора (Евклидова норма) и Нормализация

Для вектора **V** = (v1, v2, ..., vn) длина **L** вычисляется как корень из суммы квадратов его координат:

$$
L = \|\mathbf{V}\| = \sqrt{v_1^2 + v_2^2 + ... + v_n^2}
$$

Каждая координата исходного вектора делится на полученную длину:

$$
\mathbf{V}_{norm} = \left( \frac{v_1}{L}, \frac{v_2}{L}, ..., \frac{v_n}{L} \right)
$$

> **Пример:** Если вектор (3, 4), то L = 5. Нормализованный вектор: (3/5, 4/5).

---

## 5-10 Синаптическое преобразование (Скалярное произведение)

Это операция вычисления взвешенной суммы входных сигналов.

Для входного вектора **X** и весового вектора **W**:

$$
S = \mathbf{X} \cdot \mathbf{W} = \sum_{i=1}^{n} x_i \cdot w_i = x_1 w_1 + x_2 w_2 + ... + x_n w_n
$$

---

## 10-15 Обобщённое правило Хебба

Вес усиливается, если вход и выход нейрона активированы одновременно (обучение без учителя).

$$
\Delta w = \alpha \cdot x \cdot y
$$

Где:
*   `Δw` — приращение веса.
*   `α` — коэффициент обучения.
*   `x` — входное значение.
*   `y` — выходное значение.

---

## 15-20 Дельта-правило (Правило Уидроу-Хоффа)

Вес корректируется пропорционально ошибке (обучение с учителем).

$$
\Delta w = \alpha \cdot x \cdot (T - Y)
$$

Где:
*   `T` — эталонное значение.
*   `Y` — реальное выходное значение.
*   `T - Y` — ошибка.

---

## 20-25 Функции активации и их производные

### Группа 1: Восстановление функции (Зная производную y', найти функцию)
*В этих вопросах дано выражение производной, нужно назвать исходную функцию активации.*

**Какова формула функции активации в зависимости от её аргумента S, у которой производная `y' = 1`:**
- `S`

**Какова формула функции активации в зависимости от её аргумента S, значение которой и её производной связаны выражением `y' = y(1-y)`:**
- `1 / (1 + exp(-S))`

**Какова формула функции активации в зависимости от её аргумента S, значение которой и её производной связаны выражением `y' = (1+y)(1-y)`:**
- `th(S)`

**Какова формула функции активации в зависимости от её аргумента S, у которой производная `y' = 1/(1+S^2)`:**
- `arctg(S)`

**Какова формула функции активации в зависимости от её аргумента S, у которой производная `y' = 1/sqrt(1+S^2)`:**
- `arcsh(S)`

**Какова формула функции активации в зависимости от её аргумента S, у которой производная `y' = -2*S*exp(-S^2)`:**
- `exp(-S^2)`

**Какова формула функции активации в зависимости от её аргумента S, значение которой и её производной связаны выражением `y' = cos^2(y)`:**
- `arctg(S)`

**Какова формула функции активации в зависимости от её аргумента S, у которой производная `y' = ch^-2(S)`:**
- `th(S)`

**Какова формула функции активации в зависимости от её аргумента S, значение которой и её производной связаны выражением `y' = 1/ch(y)`:**
- `arcsh(S)`

---

### Группа 2: Производная, выраженная через значение функции (y)
*Дана функция, нужно найти её производную без использования S (для Backpropagation).*

**Какова формула зависимости значения производной радиально-базисной функции активации (`y = exp(-S^2)`) от значения функции активации `y`:**
- `-2 * y * sqrt(-ln(y))`

**Какова формула зависимости значения производной сигмоидной функции активации (`y = 1/(1+exp(-S))`) от значения функции активации `y`:**
- `y * (1 - y)`

**Какова формула зависимости значения производной функции активации (`y = th(S)`) от значения функции активации `y`:**
- `(1 + y) * (1 - y)`

**Какова формула зависимости значения производной функции активации (`y = arcsh(S)`) от значения функции активации `y`:**
- `1 / ch(y)`

**Какова формула зависимости значения производной функции активации (`y = arctg(S)`) от значения функции активации `y`:**
- `cos^2(y)`

---

### Группа 3: Производная, выраженная через аргумент (S)
*Дана функция, нужно найти её классическую производную по аргументу S.*

**Какова формула зависимости значения производной линейной функции активации (`y = S`) от аргумента `S`:**
- `1`

**Какова формула зависимости значения производной радиально-базисной функции активации (`y = exp(-S^2)`) от аргумента `S`:**
- `-2 * S * exp(-S^2)`

**Какова формула зависимости значения производной сигмоидной функции активации (`y=1/(1+exp(-S))`) от аргумента `S`:**
- `exp(-S) / (1 + exp(-S))^2`

**Какова формула зависимости значения производной функции активации (`y = th(S)`) от аргумента `S`:**
- `1 / ch^2(S)`

**Какова формула зависимости значения производной функции активации (`y = arcsh(S)`) от аргумента `S`:**
- `1 / sqrt(1 + S^2)`

**Какова формула зависимости значения производной функции активации (`y=arctg(S)`) от аргумента `S`:**
- `1 / (1 + S^2)`
